/**
 * Phase 1/2 foundation placeholder.
 * Phase 3 will replace this with the real page (header, converter card, content).
 * Kept deliberately minimal: proves boot, fonts, and tokens render correctly.
 */
export default function App() {
  return (
    <main className="mx-auto max-w-2xl px-4 py-8">
      <p className="text-sm text-muted-foreground">
        জমির হিসাব — foundation ready. Converter UI arrives in Phase 3.
      </p>
    </main>
  );
}
